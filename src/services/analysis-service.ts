import { injectable, inject } from "tsyringe";

@injectable()
export class AnalysisService{
    constructor(@inject())

    public analyzeData(symbol:string){
        try{
            
        }catch{
            
        }
    }
}